"""
models.py

SQLAlchemy ORM models defining the database schema. Declares tables for
receipts, line items, categories, and users. All models inherit from a common
declarative base defined in database.py.
"""
